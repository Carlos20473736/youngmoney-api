{
    auto_https off
    admin off
}

:80 {
    root * /app
    
    # Servir arquivos PHP diretamente
    file_server
    
    # Processar PHP
    php_fastcgi localhost:9000
    
    # Tentar servir arquivo, senão index.php
    try_files {path} {path}.php /index.php
}
